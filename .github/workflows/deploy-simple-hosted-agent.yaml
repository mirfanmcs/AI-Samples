name: Deploy Simple Hosted Agent

on:
  #push:
  #  paths:
  #    - 'Agentic-AI/Simple-Agent-App-Hosted-Agent/Agents/**'
  #    - 'Agentic-AI/Simple-Agent-App-Hosted-Agent/requirements.txt'
  #    - 'Agentic-AI/Simple-Agent-App-Hosted-Agent/.env.example'
  #    - '.github/workflows/deploy-simple-hosted-agent.yaml'
  workflow_dispatch:

jobs:
  Build:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.build-image.outputs.image }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2.1.1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS_AGENT_DEPLOY }}

      - name: Build and push hosted agent image
        id: build-image
        working-directory: Agentic-AI/Simple-Agent-App-Hosted-Agent
        shell: bash
        run: |
          az acr build \
            --registry "${{ vars.ACR_NAME }}" \
            --image "simple-agent-hosted:${{ github.sha }}" \
            --platform linux/amd64 \
            Agents
          ACR_LOGIN_SERVER=$(az acr show \
            --name "${{ vars.ACR_NAME }}" \
            --query loginServer \
            --output tsv)
          echo "image=${ACR_LOGIN_SERVER}/simple-agent-hosted:${{ github.sha }}" \
            >> "$GITHUB_OUTPUT"

  Deploy:
    runs-on: ubuntu-latest
    needs: Build

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create environment file from secret
        run: 'echo "$ENV" > Agentic-AI/Simple-Agent-App-Hosted-Agent/.env'
        shell: bash
        env:
          ENV: ${{ secrets.ENV }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Login to Azure
        uses: azure/login@v2.1.1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS_AGENT_DEPLOY }}

      - name: Install deployment dependencies
        working-directory: Agentic-AI/Simple-Agent-App-Hosted-Agent
        run: python -m pip install --requirement requirements.txt

      - name: Register hosted agent version with Python SDK
        working-directory: Agentic-AI/Simple-Agent-App-Hosted-Agent
        run: |
          python Agents/deploy_hosted_agent.py \
            --image "${{ needs.Build.outputs.image }}"
